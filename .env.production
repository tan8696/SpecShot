# The download ad's AdSense unit (components/AdGate.tsx). Not a secret — it's
# printed into every page that shows the ad. A value set in the host's env vars
# overrides this file.
NEXT_PUBLIC_ADSENSE_SLOT_ID=1201121707
